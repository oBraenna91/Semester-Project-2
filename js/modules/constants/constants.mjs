/**
 * This const contains the API base URL for all the unauthorized endpoints.
 */
export const API_BASE_URL = "https://api.noroff.dev/api/v1/auction/";

/**
 * This const contains the API base URL for all the authorized endpoints. 
 */
export const API_BASE_AUTH = `${API_BASE_URL}auth/`;